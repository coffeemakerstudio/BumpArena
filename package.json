{
  "name": "BumpArena",
  "version":"0.0.1",
  "private": false,
  "type": "module",
  "main": "dist/cjs/arena.js",
  "module": "dist/esm/arena.js",
  "types": "dist/arena.d.ts",
  "scripts": {
    "build:cjs": "bun build arena.ts --target bun --outdir dist/cjs --format cjs",
    "build:esm": "bun build arena.ts --target bun --outdir dist/esm --format esm",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist/types",
    "build": "npm run build:cjs && npm run build:esm && npm run build:types",
    "test": "node ./dist/esm/arena.js && node ./dist/cjs/arena.js"
  },
  "devDependencies": {
    "@types/node": "^25.2.3"
  }
}
